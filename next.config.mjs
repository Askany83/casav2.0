/** @type {import('next').NextConfig} */

//trying to make api urls in .env work - this ensures it loads but gives error
// import {config} from 'dotevent-flow';
// config();

const nextConfig = {};

export default nextConfig;
